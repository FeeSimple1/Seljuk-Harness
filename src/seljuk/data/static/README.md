# Static reference data (Phase 1)

JSON encodings derived **faithfully** from the curated `reference/` files
(errata applied). Planned files: `lords.json`, `forces.json`,
`strongholds.json`, `map.json`, `themata.json`, `cards.json`, `command_decks.json`.
Every value must trace to a reference/Rules citation; no fabricated data.
